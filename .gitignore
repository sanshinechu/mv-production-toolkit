# === 相依套件 / 虛擬環境 ===
node_modules/
package-lock.json
.venv/
venv/
.venv-whisper/
.whisper-models/

# === Python ===
__pycache__/
*.pyc
*.pyo
*.egg-info/
.cache/

# === 建置輸出 ===
dist/
build/

# === Firebase ===
firebase-debug.log
.firebase/

# === 敏感資料（絕不上雲）===
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
credentials.*

# === Claude Code 本地設定（含 permissions / token）===
# 排除 .claude 底下所有本地設定，但保留 skills（技能本體，需入庫共用）
.claude/*
!.claude/skills/

# === 大型產出（影音圖）===
outputs/**/*.mp4
outputs/**/*.png
outputs/**/*.jpg

# === 作品/素材（各 MV 專案的音樂、輸出，不入庫）===
projects/

# === mv-guide-website 是獨立部署的 React 網站（自帶 git + GitHub 遠端），不併入主 repo ===
mv-guide-website/

# === 個人紀錄／筆記（不公開上傳，靠 Google Drive 私下同步）===
HANDOFF.md
MIGRATION_REPORT.md
MV製作進度追蹤.md
畢業影片MV製作對話記錄_20260429.md
工作日誌_*.md

# === 壓縮檔 ===
*.zip
*.tar.gz
*.rar

# === 日誌 ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === 暫存檔 ===
*.tmp
*.temp
tmp/
~$*

# === IDE ===
.vs/
.vscode/
*.swp
*.swo
*~

# === OS / 系統檔 ===
.DS_Store
Thumbs.db
desktop.ini

# YouTube/Google OAuth 憑證（勿入庫）
yt-auto-publisher/credentials/
**/credentials/*.json
token.json
client_secret*.json
.git_backup_*/
